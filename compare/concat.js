const first = 'Naiem';
const last = 'Hossain';

const fullName = first + ' ' + last;
console.log(fullName);

const fullName2 = first.concat(' ', last);
console.log(fullName2);

console.log(last.includes('Hoss'));